# PostgreSQL connection string
# Example: postgresql+psycopg2://postgres:postgres@localhost:5432/lims
DATABASE_URL=postgresql+psycopg2://postgres:Namratha%402003@localhost:5432/lmis

# SMTP (optional; backend will gracefully log instead if not set)
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=
SMTP_PASS=
SMTP_FROM=lims@example.com
